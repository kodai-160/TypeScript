{
  "dependencies": {
    "@fastify/mongodb": "^9.0.2",
    "fastify": "^5.4.0",
    "fastify-cli": "^7.4.0",
    "fastify-plugin": "^5.0.1"
  }, 
  "scripts": {
    "start": "fastify start -l info -P --ignore-watch node_modules --ignore-watch package.json --ignore-watch package-lock.json"
  }
}
